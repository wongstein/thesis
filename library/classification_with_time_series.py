from library import MonteCarlo, database
import datetime

listing_reservation_dict = {}

#day
day_season_designation = {}

#for prediction
predicted_occupancy_dict = {}


def find_testing_listings():
    testing_listings = []

#full_training_dict: season_designation: reservation: created_at, end_status:

def make_training_data(listing_reservation_dict):
    full_training_dict = {}

    for day in listing_reservation_dict:
#
def per_listing_forecasting(testing_listings):
    for listing_id in testing_listings:
        #location, full_training_dict, k_iterations = 1)
        listing_monteCarlo = MonteCarlo.MonteCarlo()


def main():
    #get all data
    reservation_data_crude = get_data()
    #sort all data into proposed reservation
    #
    #
    #
    #
    #